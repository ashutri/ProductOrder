export class Login{
    id: number;
    userName: string;
    passWord: string;    
    constructor(id: number, userName: string, passWord: string){
    this.id = id;
    this.userName = userName;
    this.passWord = passWord;
    }
    }